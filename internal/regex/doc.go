// Package regex provides regular-expression helpers.
//
// It includes matching, extraction, replacement, group capture, escaping,
// and deletion utilities built on top of the standard regexp package.
// This package is exposed through the vregex facade.
package regex
